apiVersion: v1
kind: Pod
metadata:
  name: init-pod
spec:
  containers:
    - name: my-main-container
      image: nginx:1.29
      ports:
        - containerPort: 80
          protocol: TCP

  initContainers:
    - name: first-init-container
      image: alpine
      command: ["sleep","50"]

      